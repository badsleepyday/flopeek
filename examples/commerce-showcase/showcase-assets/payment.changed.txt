import { reviewRisk } from "./risk";
import { authorizeProvider } from "../payments/provider";

export async function authorizePayment(input: unknown) {
  await authorizeProvider(input);
  return reviewRisk(input);
}
